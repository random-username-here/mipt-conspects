#import "../../multibook/mb.typ": *
#let ddx = $dot(x)$
#let ddy = $dot(y)$
#let ddz = $dot(z)$
#let ddu = $dot(u)$
#let ddv = $dot(v)$

#let pdx = $pd x$
#let pdy = $pd y$
#let pdz = $pd z$
#let pdu = $pd u$
#let pdv = $pd v$

#let mmmark = (start: (
    (symbol: "straight", offset: 10%, shorten-to: none),
    (symbol: "straight", offset: 10%, shorten-to: none),
    (symbol: "straight", offset: 10%, shorten-to: none),
    (symbol: "straight", offset: 10%, shorten-to: none),
    (symbol: "straight", offset: 10%, shorten-to: none),
    (symbol: "straight", offset: 10%, shorten-to: none),
    (symbol: "straight", offset: 10%, shorten-to: none),
    (symbol: "straight", offset: 10%, shorten-to: none),
    (symbol: "straight", offset: 10%, shorten-to: none)
))

